import RegisterAuthorPage from '../features/author/register.author.page';

const routesConfig = {
    registerAuthor: {
        path: '/',
        component: RegisterAuthorPage
    }
};

export default routesConfig;
